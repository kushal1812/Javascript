let book ={
    title:'Javascript',
    author:'Andreaw Mead',
    price:420,
}

console.log(`${book.title} by ${book.author} and the price is ${book.price}`);
book.title = 'JS';
console.log(`${book.title} by ${book.author} and the price is ${book.price}`);

//challenge area
//name,age.loaction

user={
    name:'Kushal',
    age:23,
    location:'knr'
}

console.log(`${user.name} is ${user.age} lives in ${user.location}`);
user.age=24;
console.log(`${user.name} is ${user.age} lives in ${user.location}`);
